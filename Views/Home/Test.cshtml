@model List<TestItems>
@{
    ViewData["Title"] = "Test";
}

<table class="table table-info">
    <thead>
        <tr>
            <th>S/N</th>
            <th>Product Name</th>
            <th>Status of Test</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.TestName</td>
                <td>@item.TestStatus</td>
            </tr>
        }
    </tbody>
</table>